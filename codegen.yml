schema: ${API_URL}
documents: ./src/graphql/**/*.ts
generates:
  ./src/generated/codegen.d.ts:
    plugins:
      - typescript
      - typescript-react-apollo
    config:
      withComponent: false
      withHOC: false
      withHooks: true
